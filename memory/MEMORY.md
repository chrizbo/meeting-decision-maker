# Memory Index

- [RTMS Timestamp Behavior](project_rtms_timestamps.md) — RTMS webhook payload has `start_time`/`end_time` per cue; `timestamp` is a generic event time and must not be used as primary cue timestamp
